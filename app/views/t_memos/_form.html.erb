<%= form_with(model: t_memo) do |form| %>
  <% if t_memo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(t_memo.errors.count, "error") %> prohibited this t_memo from being saved:</h2>

      <ul>
        <% t_memo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :メモ, style: "display: block" %>
    <%= form.text_field :memo %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
